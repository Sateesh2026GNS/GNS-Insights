# Use SQLite (works out of the box). Switch to PostgreSQL when ready:
# DATABASE_URL=postgresql://postgres:YOUR_ACTUAL_PASSWORD@localhost:5432/smrt_db
# Password with @ must be URL-encoded as %40
DATABASE_URL=postgresql://postgres:Admin%40gnm2026!@localhost:5432/smrt_db